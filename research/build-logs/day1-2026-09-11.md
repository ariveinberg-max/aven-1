# Build log — Day 1 (2026-09-11)

## Aven-1 (58.4M, Mac)
- Checkpoint `b896a049...`, step 5499, unchanged today.
- Full evaluation sweep run against the frozen capability-v1 suite (`runs/eval-baselines/day1-plain-20260911T213229Z.json`). All five categories scored 0% under strict exact-match — consistent with documented history in `research/TASKS.md`, not a new regression. Confirmed by direct comparison: cached and non-cached generation produce byte-identical output, ruling out today's KV-cache change as the cause.
- RLHF preference labeling: 62 new comparisons logged today (438 → 500 total), past the 50-minimum target.

## Aven-2 (153M, Windows PC)
- Pretraining live and healthy: step 7240, held-out perplexity 44.89, declining steadily from the mid-40s/low-50s over the last several hundred steps. No crashes since the optimizer state-dict fix.

## Shipped today
- KV-cache inference in `brain.py` (~2.9x faster generation within the context window, verified bit-identical output vs. non-cached path).
- Checkpoint export/verify tooling (`checkpoint_bundle.py`).
- Tokenizer-fingerprint validation added to `ppo.py` and `raft.py`.
- Reward-model validation now splits by normalized prompt to prevent train/val leakage.
- Fixed a stale roadmap task that pointed eval output at the wrong file (`runs/ledger.jsonl`, a training-only log) — corrected to `runs/eval-baselines/`.
- Full local infra check: GitHub Actions CI green on the day's commit, checkpoint-backup agent fixed (needed Full Disk Access), Grafana reachable cross-machine again after a wrong-host/localhost mixup.

## Known limitations (unchanged, tracked honestly)
- Day-after-Sunday wraparound bug, occasional list-count variance — both low-priority, unfixed.
- Aven-1's capacity ceiling for layering new instruction-tuning content onto one checkpoint remains an open, undertested hypothesis.

## Next
- Research reading block (still open, personal task).
- Day 2: labeling session 2, reward-model retrain, README rewrite for first-time readers.
